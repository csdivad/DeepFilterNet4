# DeepFilterNet3-MLX

Converted MLX checkpoint for the shipped `models/DeepFilterNet3.zip` archive.

## Source

- Archive: `models/DeepFilterNet3.zip`
- Original checkpoint: `DeepFilterNet3/checkpoints/model_120.ckpt.best`
- Original config: `DeepFilterNet3/config.ini`

## Layout

- `config.ini` — original model configuration
- `checkpoints/model_120.safetensors` — converted MLX weights

## Notes

This artifact is intended for Apple Silicon / MLX workflows and preserves the original DeepFilterNet3 model configuration from the shipped archive.
